# viewer endpoint moved

`surface-viewer` is no longer an active GlassVein endpoint.

Use `../console` (`console-endpoint`) for the human TUI. The console includes
the session list/status/detail aggregation formerly provided by the viewer and
adds canonical control/request actions.
