{
  "name": "@opensessiongateway/osgp",
  "version": "0.1.3",
  "description": "OSGP protocol types, builders, and codec for the Open Session Gateway",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "check:registry": "node ./scripts/check-canonical-registry.mjs",
    "clean": "rm -rf dist"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
