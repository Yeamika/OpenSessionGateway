# OSG Server Plugin SDK

Shared SDK for server-side plugins in OpenSessionGateway.

Current scope:

- plugin manifest and lifecycle types
- MCP surface types
- host `ctx.osg` API types
- host `ctx.storage` API types
- small JSON-RPC helpers

This package is intended to be published to the local registry when needed.
