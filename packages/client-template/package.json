{
  "name": "@opensessiongateway/client-template",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test:osg": "npm run build && node dist/osg-smoke-test.js",
    "cli": "npm run build && node dist/cli.js",
    "run:cli": "npm run build && node dist/cli.js run",
    "fleet:cli": "npm run build && node dist/cli.js fleet",
    "test:osg:cli": "npm run build && node dist/cli.js test",
    "test:osg:script": "npm run build && node dist/cli.js script --test-json-file scenarios/basic-tools.json",
    "control:cli": "npm run build && node dist/cli.js control"
  },
  "dependencies": {
    "@opensessiongateway/client-library": "0.0.4",
    "@opensessiongateway/protocol-library": "0.0.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/ws": "^8.5.14",
    "typescript": "^5.8.2"
  }
}
