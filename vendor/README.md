# Vendor Evaluation

`vendor/pingora/` is a local clone of Cloudflare Pingora for data-plane evaluation.

It is intentionally ignored by `GlassVein/.gitignore`; keep GlassVein source code in `crates/` and `examples/`.
