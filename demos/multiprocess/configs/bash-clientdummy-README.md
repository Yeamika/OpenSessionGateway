# bash-clientdummy instance configs for multiprocess demo
#
# Each file is a shell snippet showing the CLI args for one instance.
# These are reference templates — the actual launch is done by stage scripts.
#
# All instances connect to their respective routers and use the
# `bash-clientdummy` binary (built via `cargo build -p bash-clientdummy`).
#
# No real credentials. Demo-only configuration.
